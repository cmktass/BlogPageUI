@model BlogPageMvc.Models.Controller.ControllerActionVM

@{
    ViewData["Title"] = "DeleteAction";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container">
    <h1>Metot Sil</h1>
    <h3>Silmek istediğine Emin misin?</h3>
    <div>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.ActionName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.ActionName)
            </dd>
        </dl>

        <form>
            <input type="submit" value="Delete" asp-action="DeleteActionId" asp-route-id="@Model.Id" class="btn btn-danger" /> |
            <a asp-action="ControllerDetail" asp-route-id="@Model.ControllerId">Back to List</a>
        </form>
    </div>
</div>



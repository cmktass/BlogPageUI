@model BlogPageMvc.Models.Controller.ControllerVM

@{
    ViewData["Title"] = "DeleteController";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="container">
    <h1>Controller Sil</h1>
    <h3>Silmek istediğine Emin misin?</h3>
    <div>
        <h4>ControllerVM</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.ControllerName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.ControllerName)
            </dd>
        </dl>

        <form asp-action="DeleteController" asp-route-id="@Model.Id">
            <input type="submit" value="Sil" class="btn btn-danger"> |
            <a asp-action="GetAllController">Controller Listesi</a>
        </form>
    </div>
</div>

